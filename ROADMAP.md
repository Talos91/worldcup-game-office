# Roadmap

## Now — finish go-live
- [ ] Push to github.com/Talos91/worldcup-game-office and enable Pages
      (Settings → Pages → Deploy from a branch → main / root).
- [ ] Add repo secret `FOOTBALL_DATA_API_KEY` (Settings → Secrets and variables
      → Actions) so the GitHub Actions updater can fetch results.
- [ ] (Optional) Run the workflow once from the Actions tab to publish immediately.

## Decide before knockouts (~early July 2026)
- [ ] Penalty-shootout scoring: currently a shootout counts as Win(+3)/Loss(−1)
      for the winner/loser (the page is already wired to show "pens X–Y").

## Optional / nice-to-have
- [ ] Per-manager detail view / share card.
- [ ] "Manager of the matchday" highlight.
- [ ] Knockout bracket view.
